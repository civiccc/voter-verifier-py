# This gets file URLs from HDFS
import requests
import os
import sys

def get_filenames():
    * GET SOURCE FILE FILENAMES HERE, YIELD 1 by 1*

    yield filename

if __name__ == '__main__':
  for filename in get_filenames():
    print filename
