{
  "id": "verifier",
  "cmd": "env/bin/gunicorn web:app -b 0.0.0.0:3000 --log-file=-",
  "mem": 256,
  "cpus": 0.2,
  "instances": 1,
  "env": {
    "FLASK_ENV": "production"
  },
  "container": {
    "type": "DOCKER",
    "docker": {
***REMOVED***
      "network": "BRIDGE",
      "portMappings": [
        {
          "containerPort": 3000,
          "hostPort": 0,
          "protocol": "tcp"
        }
      ]
    }
  }
}
